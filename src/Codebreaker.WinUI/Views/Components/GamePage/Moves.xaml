<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CodeBreaker.WinUI.Views.Components.GamePage.Moves"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CodeBreaker.WinUI.Views.Components.GamePage"
    xmlns:converters="using:CodeBreaker.WinUI.Converters"
    xmlns:vm="using:Codebreaker.ViewModels.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:ColornameToBrushConverter x:Key="ColorConverter" />
        <DataTemplate x:Key="PegsTemplate" x:DataType="vm:Move">
            <Grid
                Background="{ThemeResource CardBackgroundFillColorDefault}"
                CornerRadius="{ThemeResource ControlCornerRadius}"
                Margin="0,16,0,0"
                Padding="12"
                ColumnSpacing="24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" x:Name="ShadowTarget">
                    <ItemsControl ItemsSource="{x:Bind GuessPegs, Mode=OneTime}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" Spacing="12"  />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="vm:Field">
                                <local:Peg 
                                    ColorName="{x:Bind Color}"
                                    ShapeName="{x:Bind Shape}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>

                <Border Grid.Column="1" >
                    <ItemsControl ItemsSource="{Binding KeyPegs, Mode=OneTime}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" Spacing="20" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Ellipse
                                    Width="25" Height="25"
                                    Fill="{Binding Mode=OneTime, Converter={StaticResource ColorConverter}}"
                                    Stroke="{ThemeResource TextBoxBorderThemeBrush}"
                                    StrokeThickness="1" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <ScrollViewer x:Name="ScrollViewer">
        <ListBox
            x:Name="listGameMoves"
            Padding="-16,0,0,0"
            Background="Transparent"
            SelectedIndex="-1"
            IsHitTestVisible="False"
            ItemsSource="{x:Bind ViewModel.Game.Moves, Mode=OneWay}"
            ItemTemplate="{StaticResource PegsTemplate}" />
    </ScrollViewer>
</UserControl>
