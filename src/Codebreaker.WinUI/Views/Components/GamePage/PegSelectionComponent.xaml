<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CodeBreaker.WinUI.Views.Components.GamePage.PegSelectionComponent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vmComponents="using:Codebreaker.ViewModels.Components"
    xmlns:vm="using:Codebreaker.ViewModels.Models"
    xmlns:toolkitConverters="using:CommunityToolkit.WinUI.UI.Converters"
    xmlns:converters="using:CodeBreaker.WinUI.Converters"
    xmlns:cme="using:CodeBreaker.WinUI.CustomMarkupExtensions"
    xmlns:dts="using:CodeBreaker.WinUI.DataTemplateSelectors"
    xmlns:gameComponents="using:CodeBreaker.WinUI.Views.Components.GamePage"
    xmlns:shapes="using:CodeBreaker.WinUI.Views.Components.GamePage.Shapes"
    mc:Ignorable="d"
    x:Name="ThisControl">
    <UserControl.Resources>
        <converters:ColornameToBrushConverter x:Key="ColorConverter" />
        <toolkitConverters:BoolToVisibilityConverter x:Key="BoolToVisibility" />
    </UserControl.Resources>

    <Grid ColumnSpacing="24" Padding="12,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <ItemsControl
            Grid.Row="1"
            Grid.Column="0"
            ItemsSource="{x:Bind Path=ViewModel.SelectedFields, Mode=OneWay}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="12" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="vm:Field">
                    <gameComponents:Peg
                        ColorName="{x:Bind Color, Mode=TwoWay}"
                        ShapeName="{x:Bind Shape, Mode=TwoWay}"
                        AllowDrop="True"
                        DragOver="PegDragOver"
                        Drop="PegDrop"
                        IsDraggable="True"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Button
            Grid.Row="1" Grid.Column="1"
            VerticalAlignment="Top"
            Padding="12"
            Command="{x:Bind ViewModel.MakeMoveCommand, Mode=OneTime}">
            <StackPanel Orientation="Vertical" Spacing="8">
                <SymbolIcon Symbol="Send" Visibility="{x:Bind ViewModel.MakeMoveCommand.IsRunning, Mode=OneWay, Converter={StaticResource BoolToVisibility}, ConverterParameter=True}" />
                <ProgressRing Visibility="{x:Bind ViewModel.MakeMoveCommand.IsRunning, Mode=OneWay}" />
                <TextBlock Text="{cme:ResourceString Name=PegSelection_MakeMove}" />
            </StackPanel>
        </Button>
    </Grid>
</UserControl>
